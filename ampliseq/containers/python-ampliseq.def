Bootstrap: docker
From: python:3.12-slim

%post
    apt-get update && apt-get install -y --no-install-recommends procps && rm -rf /var/lib/apt/lists/*
    pip install --no-cache-dir pandas==2.2.3

%labels
    description "Python 3.12 with pandas for ampliseq-cbioportal bin scripts"
