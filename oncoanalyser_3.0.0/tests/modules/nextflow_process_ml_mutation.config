// PROCESS_ML_MUTATION carries the process_medium label (36 GB * task.attempt),
// which exceeds small test/dev machines. The fixture here is a handful of rows,
// so a much smaller cap is plenty.
process {
    withLabel:process_medium { memory = 1.GB }
}
